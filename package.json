{
  "name": "sublink-local",
  "version": "1.0.0",
  "description": "A localized and containerized version of sublink-worker.",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "vitest"
  },
  "devDependencies": {
    "vitest": "^3.0.0 <3.1.0"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "js-yaml": "^4.1.0"
  }
}
